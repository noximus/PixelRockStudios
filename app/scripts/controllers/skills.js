'use strict';

/**
 * @ngdoc function
 * @name pixelRockStudiosApp.controller:SkillsCtrl
 * @description
 * # SkillsCtrl
 * Controller of the pixelRockStudiosApp
 */
angular.module('pixelRockStudiosApp')
  .controller('SkillsCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
