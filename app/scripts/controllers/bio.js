'use strict';

/**
 * @ngdoc function
 * @name pixelRockStudiosApp.controller:BioCtrl
 * @description
 * # BioCtrl
 * Controller of the pixelRockStudiosApp
 */
angular.module('pixelRockStudiosApp')
  .controller('BioCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
